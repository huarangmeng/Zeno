# category: incremental-fail
# purpose: cache keys must include trust configuration

[cacheKey]
compiler = "zeno-stage0 0.1.0"
target = "x86_64-unknown-linux-gnu"
manifestHash = "sha256:manifest"
lockHash = "sha256:lock"
sourceHash = "sha256:source"
# expected-error: cache key is missing trust configuration

[changedInput]
trustBefore = []
trustAfter = ["ffi"]
