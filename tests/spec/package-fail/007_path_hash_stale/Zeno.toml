# category: package-fail
# purpose: frozen local lockfiles reject stale path package hashes

[package]
name = "app"
version = "0.1.0"

[target]
profile = "freestanding"

[allocator]
default = false

[panic]
strategy = "trap"
stack = "none"

[oom]
strategy = "trap"
