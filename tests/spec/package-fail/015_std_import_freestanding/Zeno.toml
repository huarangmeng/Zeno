# category: package-fail
# stage: mvp
# milestone: M2
# purpose: std imports require hosted profile or an explicit hosted-capable builtin dependency

[package]
name = "stdFreestanding"
kind = "library"

[target]
triple = "x86_64-unknown-linux-gnu"
profile = "freestanding"

[allocator]
default = false

[panic]
strategy = "trap"
stack = "none"

[oom]
strategy = "trap"

[trust]
ffi = false
rawMemory = false
hardware = false
inlineAsm = false
interrupts = false
dependencyTrust = false
