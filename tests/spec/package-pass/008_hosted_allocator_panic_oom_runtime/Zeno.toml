# category: package-pass
# feature: build-artifact
# target: x86_64-unknown-linux-gnu
# profile: hosted
# build-artifact: application
# zmeta-contains: runtimeNeeds = ["allocator", "oom", "panic"]
# zmeta-contains: linkRuntimeNeeds = ["allocator", "oom", "panic"]
# zmeta-contains: runtime=fnv1a64:
# purpose: reachable allocation, panic, and OOM paths are recorded as explicit runtime link needs

[package]
name = "hostedRuntime"
version = "0.1.0"
kind = "application"

[dependencies]
alloc = "builtin"

[target]
triple = "x86_64-unknown-linux-gnu"
profile = "hosted"

[allocator]
default = true
symbol = "std.alloc.system"

[panic]
strategy = "abort"
stack = "symbols"

[oom]
strategy = "abort"

[trust]
ffi = false
rawMemory = false
hardware = false
inlineAsm = false
interrupts = false
dependencyTrust = false
