# Zeno alloc builtin package

Stage0 treats this directory as the reserved `alloc` declaration package.
It records owning collection, string, box, and shared-owner signatures plus
allocator runtime requirements. The package is declaration-only for now.
