# Zeno std builtin package

Stage0 treats this directory as the reserved hosted `std` declaration package.
It records thread, task, sync, and atomic signatures plus their independent
runtime requirements. The package is declaration-only for now.
